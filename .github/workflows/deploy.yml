name: Build and Deploy with Toucan

on:
  push:
    tags:
      - 'v*'
      - '[0-9]*'

jobs:
  call-toucan-workflow:
    uses: BinaryBirds/github-workflows/.github/workflows/toucan_deploy.yml@main
    permissions:
      contents: read
      pages: write
      id-token: write