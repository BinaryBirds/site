{{<html}}
{{$main}}

{{& page.contents.html}}

<div id="tags" class="wrapper">

    {{#empty(context.tags)}}
    Empty.
    {{/empty(context.tags)}}
    {{^empty(context.tags)}}
    <div class="grid grid-221">
    {{#context.tags}}
        {{> partials.blog.tag}}
    {{/context.tags}}
    
    </div>
    {{/empty(context.tags)}}
</div>

{{/main}}
{{/html}}
