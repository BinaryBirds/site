{{<html}}
{{$main}}
<section>
    <div class="wrapper">
        <div id="author-page">
            <header>
                <div class="profile-picture">{{#page.imageUrl}}<img class="large rounded" src="{{page.imageUrl}}" alt="{{page.title}}">{{/page.imageUrl}}</div>
                <h1>{{page.title}}</h1>
                <hr>
                <p>{{page.description}}</p>
                <p>{{count(page.posts)}} posts</p>
            </header>

            {{& page.contents}}
            
            {{#empty(page.posts)}}
            
            {{/empty(page.posts)}}
            {{^empty(page.posts)}}
            <div class="grid grid-221">
            {{#page.posts}}
                
                {{> partials.blog.post}}
            {{/page.posts}}
            </div>
            {{/empty(page.posts)}}
            
        </div>
    </div>
</section>
{{/main}}
{{/html}}
