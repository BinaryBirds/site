{{<html}}
{{$main}}
<section>
    <div class="wrapper">
        <div id="tag">
            
            <header>
                <div class="tag-title">
                    
                    <h1>{{page.title}}</h1>
                </div>
                <hr>
                <p>{{page.description}}</p>
                <p>{{count(page.posts)}} posts</p>
            </header>

            {{& page.contents}}
            
            {{#empty(page.posts)}}
            Empty.
            {{/empty(page.posts)}}
            {{^empty(page.posts)}}
            <div class="grid grid-321">
            {{#page.posts}}
                {{> partials.blog.post}}
            {{/page.posts}}
            </div>
            {{/empty(page.posts)}}
        </div>
    </div>
</section>
{{/main}}
{{/html}}
