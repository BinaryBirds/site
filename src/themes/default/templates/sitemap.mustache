<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {{#empty(urls)}}
    {{/empty(urls)}}
    {{^empty(urls)}}
    <url>
        {{#page.pages}}
            <loc>{{permalink}}</loc>
            <lastmod>{{lastUpdate.formats.sitemap}}</lastmod>
        {{/page.pages}}

        {{#page.posts}}
            <loc>{{permalink}}</loc>
            <lastmod>{{lastUpdate.formats.sitemap}}</lastmod>
        {{/page.posts}}

        {{#page.authors}}
            <loc>{{permalink}}</loc>
            <lastmod>{{lastUpdate.formats.sitemap}}</lastmod>
        {{/page.authors}}

        {{#page.tags}}
            <loc>{{permalink}}</loc>
            <lastmod>{{lastUpdate.formats.sitemap}}</lastmod>
        {{/page.tags}}
    </url>
    {{/empty(urls)}}
</urlset>
