id: post
api: posts
paths:
    - blog/posts

properties:
    featured:
        type: bool
        required: true
        default: false

relations:
    authors:
        references: author
        type: many
        order:
            key: title
            direction: asc
    tags:
        references: tag
        type: many
        order:
            key: title
            direction: asc

queries:
    prev:
        contentType: post
        scope: detail
        limit: 1
        orderBy:
            - key: publication
              direction: desc
    next:
        contentType: post
        scope: detail
        limit: 1
        orderBy:
            - key: publication
              direction: desc
    related:
        contentType: post
        limit: 4
        filter:
            key: tags
            operator: matching
            value: "{{tags}}"